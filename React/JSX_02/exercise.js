// Write a function that receives a `name` argument and returns a `h1` tag with the `name` variable appended to the `"Hello, "` string.
import React from "react";

const PrintName = (n) => <h1>Hello, {n} </h1>;
